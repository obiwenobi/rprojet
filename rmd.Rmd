---
title: "testproj"
output:
  html_document: default
  pdf_document: default
date: "2024-12-06"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

JE voulais ajouter que je mange du pain. Je fais un graphique en barre

```{r}
hist(iris$Sepal.Length)
```

Et maintenant je vais manger des croques pour caler le café.

Je me demande s'il existe un lien entre deux variables

```{r}
plot(iris)
```

test

Une simple régression linéaire nous dirait rapidement de quoi il s'agit.

```{r,echo =TRUE}

lm(iris$Sepal.Length~iris$Petal.Length) %>% summary
```
